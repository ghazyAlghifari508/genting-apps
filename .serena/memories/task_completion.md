# Task Completion Checklist
1. Run `npm run lint` to ensure no linting errors.
2. Run `npm run typecheck` to verify type safety.
3. Test AI features (Vision, Chat, Quiz) with the configured OpenRouter models.
4. Verify data persistence in Supabase/localStorage.
